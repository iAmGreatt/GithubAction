name: First Workflow

on: push

jobs:
  build:
    run-ons: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run a sample github action
      - run: echo "This workflow run successfully on the particular task"
